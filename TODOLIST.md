# ToDoList — Chrome 网页分区截图插件

> 目标：创建一个 Chrome 插件（Manifest V3），将网页手动框选为多个部分，并将每个部分保存为 PNG 图片。
> 技术栈：原生 JavaScript（JS）+ HTML + CSS，符合 Chrome 插件开发标准，代码高内聚低耦合。

---

## 任务总览（Checkbox）

- [x] **任务 0：需求分析与技术方案调研**（已完成，详见本文件末尾“技术方案说明”）

### 一、项目初始化与环境搭建

- [x] **任务 1：初始化插件项目结构**
  - 子任务 1.1：创建 `manifest.json`，版本声明为 V3
  - 子任务 1.2：创建 `background/`（Service Worker）、`content/`（页面脚本）、`popup/`（弹窗）、`assets/`（图标）目录
  - 子任务 1.3：创建各目录占位文件，保证插件可被 Chrome 加载
  - 子任务 1.4：编写 `README.md`，说明本地加载插件（`chrome://extensions` → 开发者模式 → 加载已解压的扩展程序）方式

- [x] **任务 2：配置 Manifest V3 核心文件**
  - 子任务 2.1：声明 `manifest_version: 3`、`name`、`version`、`description`
  - 子任务 2.2：声明 `permissions`（如 `activeTab`、`scripting`、`downloads`、`storage`）
  - 子任务 2.3：声明 `host_permissions`（`<all_urls>` 或目标网站）
  - 子任务 2.4：注册 `background.service_worker`
  - 子任务 2.5：注册 `content_scripts`（匹配所有页面，注入 JS/CSS）
  - 子任务 2.6：注册 `action`（点击图标打开 Popup）
  - 子任务 2.7：注册插件图标（16/32/48/128 px）

### 二、核心功能 — 标尺与参考线（Photoshop 同原理）

- [x] **任务 3：实现页面顶部与最左侧的标尺栏**
  - 子任务 3.1：在页面顶部插入横向标尺（X 轴），单位像素，带刻度线与数字
  - 子任务 3.2：在页面最左侧插入纵向标尺（Y 轴），单位像素，带刻度线与数字
  - 子任务 3.3：实现高 DPI / 缩放（`devicePixelRatio`）下的刻度自适应
  - 子任务 3.4：标尺样式美观，使用半透明遮罩层（`content` 脚本内 Shadow DOM 隔离样式）
  - 子任务 3.5：提供标尺的显示/隐藏切换开关

- [x] **任务 4：实现参考线的拖拽创建**
  - 子任务 4.1：按住鼠标左键从顶部 X 轴标尺拖动，创建横向参考线（水平横线）
  - 子任务 4.2：按住鼠标左键从左侧 Y 轴标尺拖动，创建纵向参考线（垂直竖线）
  - 子任务 4.3：拖拽过程中实时显示参考线的当前位置（像素坐标提示）
  - 子任务 4.4：松开鼠标后参考线固定到当前位置，并在标尺上标注其坐标
  - 子任务 4.5：参考线以文档坐标定位，随页面滚动而移动（贴在页面内容上）

- [x] **任务 5：参考线管理与交互**
  - 子任务 5.1：支持按住已有参考线拖动，实时修改位置
  - 子任务 5.2：点击参考线时，在其旁边显示删除按钮
  - 子任务 5.3：点击删除按钮，移除该条参考线
  - 子任务 5.4：提供“一键清空所有参考线”的按钮
  - 子任务 5.5：参考线位置数据持久化保存到 `chrome.storage`，页面刷新后恢复

### 三、核心功能 — 分区截图保存 PNG

- [x] **任务 6：实现分区裁剪逻辑**
  - 子任务 6.1：根据参考线网格计算所有分区块（cells）的坐标与尺寸
  - 子任务 6.2：忽略尺寸过小的区块（如重叠或异常区块）
  - 子任务 6.3：将每个区块坐标转换为裁剪参数，按序排列生成任务队列

- [x] **任务 7：实现整页截图与按参考线裁剪（PNG 输出）**
  - 子任务 7.1：计算文档完整尺寸（`scrollWidth` / `scrollHeight`，含不可见区域）
  - 子任务 7.2：将整页按视口划分为网格，逐格滚动并调用 `chrome.tabs.captureVisibleTab` 截取真实画面
  - 子任务 7.3：将全部视口片段按文档坐标拼接成整页 canvas（可见 + 不可见部分全覆盖）
  - 子任务 7.4：以参考线为切分标准，对每个区块从整页 canvas 进行像素级裁剪（`canvas.drawImage` + `toDataURL('image/png')`）
  - 子任务 7.5：将裁剪结果转换为 Blob，通过 `chrome.downloads` 触发保存；完成后恢复原滚动位置

- [x] **任务 8：实现 PNG 命名规则**
  - 子任务 8.1：从页面 URL 解析网页名称（如去掉协议与域名后缀）
  - 子任务 8.2：生成命名格式 `序号_网页名称.png`（如 `1_mywebsite.png`）
  - 子任务 8.3：处理非法文件名字符与重名冲突（自动追加时间戳）

### 四、进度与界面交互

- [x] **任务 9：实现保存进度模态框**
  - 子任务 9.1：创建模态框 UI，展示“当前进度 / 总区块数”
  - 子任务 9.2：实现详细进度条（已保存页数百分比）
  - 子任务 9.3：逐张保存时实时更新进度
  - 子任务 9.4：提供“取消”按钮，支持中断保存流程
  - 子任务 9.5：保存完成后显示完成提示与汇总信息

- [x] **任务 10：实现插件控制面板（Popup + 悬浮按钮）**
  - 子任务 10.1：创建 Popup 界面，包含“开始截图”、“一键清空标尺”、“显示/隐藏标尺”等按钮
  - 子任务 10.2：提供“开始裁切 PNG”按钮，点击后启动截图流程
  - 子任务 10.3：通过 `chrome.runtime` 消息机制实现 Popup 与 Content Script 通信
  - 子任务 10.4：页面内提供浮动工具条，便于不打开 Popup 直接操作

### 五、代码质量与工程化

- [x] **任务 11：代码高内聚低耦合设计**
  - 子任务 11.1：将标尺绘制、参考线管理、截图裁剪、命名、UI 拆分为独立模块（ES Module）
  - 子任务 11.2：使用统一的模块间消息/事件接口，避免全局变量污染
  - 子任务 11.3：为各模块编写清晰的职责注释与 JSDoc
  - 子任务 11.4：遵循 Chrome 插件 MV3 标准（Service Worker 生命周期、消息传递、权限最小化）

- [x] **任务 12：UI 美化与边界处理**
  - 子任务 12.1：整体 UI 采用现代化设计（圆角、阴影、渐变、合适的字体）
  - 子任务 12.2：适配浅色/深色页面背景，保证标尺与工具条可读
  - 子任务 12.3：处理页面滚动时参考线定位（使用固定定位与滚动偏移计算）
  - 子任务 12.4：处理 iframe、跨域图片（canvas 污染）等异常并给出友好提示

### 六、测试与发布

- [x] **任务 13：功能测试**（已通过静态校验与单元验证，见下方"已完成项"）
  - 已完成项：所有 JS 语法通过 `node --check`；`manifest.json` 通过 JSON 校验且引用文件齐全；命名规则与分区算法通过单元验证
  - 待真机验证（需在 Chrome 中加载插件实测）：
  - 子任务 13.1：在多种典型页面（长页面、滚动页、有 fixed 元素页）测试截图
  - 子任务 13.2：测试参考线拖拽、修改位置、删除、一键清空
  - 子任务 13.3：测试进度模态框、取消操作、重复命名
  - 子任务 13.4：验证浏览器控制台无报错、无内存泄漏

- [x] **任务 14：打包与发布准备**
  - 子任务 14.1：压缩插件为 zip 包
  - 子任务 14.2：编写最终版 `README.md`（含使用说明、截图、功能列表）
  - 子任务 14.3：检查 Manifest 与代码符合 Chrome 应用商店审核规范

---

## 技术方案说明

### 推荐的业界通用截图库

| 库名 | 用途 | 说明 |
| --- | --- | --- |
| **html2canvas** | 整页/元素截图 | 最常用的纯 JS 截图库，可将 DOM 渲染为 canvas，适合将网页保存为 PNG；注意对跨域图片与部分 CSS（如 `filter`）支持有限 |
| **html-to-image** | 整页/元素截图 | 基于 SVG `foreignObject` 实现，渲染质量更高、支持更多 CSS 特性，是 html2canvas 的优秀替代方案 |
| **dom-to-image** | 整页/元素截图 | 同为 SVG `foreignObject` 方案，较老但稳定，可作为备选 |

> 补充说明：本插件实际采用“**视口截图 + 像素级裁剪**”方案，即以 `chrome.tabs.captureVisibleTab` 获取所见即所得的真实渲染画面作为底图，再用 `canvas.drawImage` 对每个分区坐标进行精确裁剪输出 PNG。这一方案避免了 html2canvas 对复杂 CSS 的渲染失真问题，是 Chrome 插件场景下最高保真的做法。html2canvas / html-to-image 可作为整页超长内容截图的补充方案（需配合滚动拼接）。

### 涉及的核心 Chrome API

- `chrome.runtime` — 消息传递（Popup ↔ Content Script ↔ Background）
- `chrome.tabs.captureVisibleTab` — 获取当前视口截图
- `chrome.downloads` — 触发 PNG 文件下载保存
- `chrome.storage` — 持久化参考线位置等配置
- `chrome.scripting` — 向页面注入脚本
- `chrome.action` — 配置浏览器工具栏按钮与 Popup
